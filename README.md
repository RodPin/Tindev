# Tindev
Omnistack 8.0 

# how to use
### Run the server
   cd backend && yarn && yarn dev 

### Run web front end
   cd webfrontend && yarn && yarn start

### Run Mobile App
   cd tindevRN && yarn && react-native run-android
  
  
